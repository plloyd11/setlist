---
phase: 05-landing-page-structure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - static/images/screenshots/setlist-timing.svg
  - static/images/screenshots/song-library.svg
  - static/images/screenshots/band-collaboration.svg
autonomous: false
requirements:
  - FEAT-02
gap_closure: true

must_haves:
  truths:
    - "Feature section images show realistic representations of the app UI, not placeholder labels"
    - "Setlist timing illustration depicts a song list with durations and a timing bar at the bottom"
    - "Song library illustration depicts a searchable list of songs with titles and durations"
    - "Band collaboration illustration depicts a band workspace with stats, members, and shared setlists"
  artifacts:
    - path: "static/images/screenshots/setlist-timing.svg"
      provides: "Realistic SVG illustration of the setlist builder with timing bar"
      min_lines: 40
    - path: "static/images/screenshots/song-library.svg"
      provides: "Realistic SVG illustration of the song library page"
      min_lines: 40
    - path: "static/images/screenshots/band-collaboration.svg"
      provides: "Realistic SVG illustration of the band workspace dashboard"
      min_lines: 40
  key_links:
    - from: "src/routes/+page.svelte"
      to: "static/images/screenshots/"
      via: "img src references unchanged — same paths, new content"
      pattern: "src.*images/screenshots"
---

<objective>
Replace the three SVG placeholder screenshot files with detailed, realistic SVG illustrations that depict the actual app UI. This closes the FEAT-02 gap identified in verification — "Real app screenshots embedded in feature cards."

Purpose: The landing page feature sections currently show labeled placeholder rectangles with "Screenshot placeholder" text. Visitors need to see what the app actually looks like. Since we cannot capture browser screenshots via CLI, we create hand-crafted SVG illustrations that faithfully represent the real UI using the app's actual color palette and layout patterns.

Output: Three SVG files (800x500) that look like stylized app screenshots, using the dark theme palette (surface-800/900/950, accent-400, neon-300).
</objective>

<execution_context>
@/Users/pete/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pete/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-landing-page-structure/05-VERIFICATION.md
@src/routes/layout.css
@src/routes/(app)/bands/[id]/setlists/[setlistId]/+page.svelte
@src/routes/(app)/bands/[id]/songs/+page.svelte
@src/routes/(app)/bands/[id]/+page.svelte
@src/lib/components/setlists/SetlistSongRow.svelte
@src/lib/components/setlists/TimingBar.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realistic SVG illustrations for all three feature screenshots</name>
  <files>
    static/images/screenshots/setlist-timing.svg
    static/images/screenshots/song-library.svg
    static/images/screenshots/band-collaboration.svg
  </files>
  <action>
    Replace the three placeholder SVG files with detailed illustrations of the actual app UI. Each SVG is 800x500 with viewBox="0 0 800 500". Use the app's dark theme palette throughout:

    - Background: #080c13 (surface-950) or #0f1720 (surface-900)
    - Cards/panels: #162030 (surface-800) with #1e2a3d (surface-700) borders
    - Text primary: #d0dce8 (surface-100)
    - Text secondary: #5a7190 (surface-400)
    - Text muted: #3d5170 (surface-500)
    - Accent/copper: #a78265 (accent-400)
    - Neon/chartreuse: #d1d895 (neon-300)
    - Danger: #c95640 (danger-400)
    - Success: #94b355 (success-300)

    Use font-family="sans-serif" throughout (SVG cannot use custom fonts).

    **1. setlist-timing.svg — Setlist Builder with Timing**

    Depict the two-panel setlist builder layout as seen in the actual app:

    LEFT PANEL (narrow, ~240px wide): "Band Library" header, a search input, then 5-6 song rows. Each row is a rounded rect card with song title on left and duration (like "3:45") on right. Use realistic song names like "Midnight Run", "Electric Feel", "Fade to Black", "Under Pressure", "Bohemian Rhapsody", "Don't Stop Me Now". Songs already in the setlist should appear at 50% opacity.

    RIGHT PANEL (wider, rest of space): "Friday Night at The Roxy" as the setlist name header in accent-400. Below that, 4 song rows with drag handles (6-dot grip icon on left), song title, and duration. Each row is a rounded card with surface-800 background and surface-700 border. Show songs: "Midnight Run 4:12", "Electric Feel 3:45", "Under Pressure 3:38", "Bohemian Rhapsody 5:55".

    BOTTOM BAR (full width, sticky): The timing bar. Show: "Total 17:30" on left in bold, "Target 45:00" with an input-style box, "Diff -27:30" in success green, and a progress bar (about 39% filled) in accent-400 on surface-700 background, and "Gap 15s" stepper on right.

    Include a subtle vertical border (#1e2a3d) between the two panels.

    **2. song-library.svg — Song Library Page**

    Depict the band songs page:

    TOP: "Songs" header on the left, "Share from Library" and "Add New" buttons on the right. "Add New" button uses accent-500 (#8a6a4f) background with white text.

    SEARCH: A full-width search input below the header with placeholder text "Search band songs..."

    SONG LIST: A rounded container (surface-800 with surface-700 border) with 6 song rows separated by surface-700 dividers. Each row shows: song title (surface-100, left-aligned), duration (surface-400, right-aligned), and a small X button (surface-500) at far right. Use realistic data:
    - "Midnight Run" — "4:12"
    - "Electric Feel" — "3:45"
    - "Fade to Black" — "6:26"
    - "Under Pressure" — "3:38"
    - "Bohemian Rhapsody" — "5:55"
    - "Don't Stop Me Now" — "3:29"

    One row (e.g., "Fade to Black") can show a notes line below the title in surface-500: "Drop D tuning"

    **3. band-collaboration.svg — Band Workspace Dashboard**

    Depict the band overview page:

    STATS ROW: Three stat cards side by side in a grid. Each is a rounded rect (surface-800, surface-700 border) with a large number in accent-500 (#8a6a4f) and a label below in surface-400:
    - "4" / "Members"
    - "12" / "Songs"
    - "3" / "Setlists"

    ACTION BUTTONS: Below stats, show three pill-shaped buttons: "Add Songs", "Create Setlist", "Invite Members" — each with a small icon and surface-800 background with surface-700 border.

    RECENT SETLISTS: "Recent Setlists" header, then 3 setlist rows as rounded cards (surface-800, surface-700 border). Each shows setlist name on left (surface-100) and venue + date on right (surface-400):
    - "Friday Night Set" — "The Roxy -- Feb 28"
    - "Acoustic Unplugged" — "Blue Note -- Mar 5"
    - "Festival Main Stage" — "Lakeside Park -- Mar 15"

    All three SVGs should:
    - Have a 12px rounded outer border (rx="12") with surface-800 fill
    - Use consistent spacing and sizing
    - NOT contain any text saying "placeholder" or "screenshot"
    - Look like a realistic dark-mode app UI, not a wireframe
    - Use rounded rects (rx="6" to rx="8") for cards and inputs to match the app's rounded-lg style
  </action>
  <verify>
    Run `grep -l "placeholder" static/images/screenshots/*.svg` — should return NO results (no placeholder text remaining).
    Run `grep -c "rect\|text\|circle\|line\|path" static/images/screenshots/setlist-timing.svg` — should show 30+ SVG elements (not a simple placeholder).
    Run `grep -c "rect\|text\|circle\|line\|path" static/images/screenshots/song-library.svg` — should show 30+ SVG elements.
    Run `grep -c "rect\|text\|circle\|line\|path" static/images/screenshots/band-collaboration.svg` — should show 30+ SVG elements.
    Run `grep "Midnight Run\|Electric Feel\|Under Pressure" static/images/screenshots/setlist-timing.svg` — confirms realistic song names present.
    Run `grep "Songs" static/images/screenshots/song-library.svg` — confirms proper header.
    Run `grep "Members\|Setlists" static/images/screenshots/band-collaboration.svg` — confirms stat labels.
  </verify>
  <done>All three SVG files contain detailed, realistic illustrations of the app UI using the dark theme palette. No placeholder text remains. Each file has 30+ SVG elements depicting actual UI components (song rows, timing bar, stat cards, etc.).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SVG illustrations look realistic on the landing page</name>
  <what-built>Three SVG illustrations replacing placeholder screenshots in the landing page feature sections. Each depicts a realistic view of the app: setlist builder with timing, song library, and band workspace.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173/ in an incognito/private browser window (logged out)
    2. Scroll to the feature sections (below the hero)
    3. Verify each illustration:
       - Feature 1 (Setlist Timing): Shows a two-panel layout with song library on left, setlist on right, and timing bar at bottom. Should look like a realistic dark-mode app screenshot, not a wireframe or placeholder.
       - Feature 2 (Song Library): Shows a song list with titles, durations, search bar, and action buttons. Should look like a real app page.
       - Feature 3 (Band Collaboration): Shows stats cards, action buttons, and recent setlists. Should look like a real band dashboard.
    4. Check that no illustration contains "placeholder" or "Screenshot placeholder" text
    5. Check that the illustrations match the dark navy/copper/chartreuse color scheme of the page
    6. Check that illustrations look good at both desktop and mobile widths (resize browser)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -rL "placeholder" static/images/screenshots/*.svg` lists all three files (none contain placeholder text)
2. Each SVG has 30+ elements depicting realistic UI components
3. SVGs use the app's actual color palette (#080c13, #162030, #a78265, #d1d895, etc.)
4. Landing page at `/` renders the illustrations in feature sections without any code changes to +page.svelte
5. Human has verified visual quality of illustrations on the live page
</verification>

<success_criteria>
- All three SVG placeholder files replaced with realistic app UI illustrations
- No "placeholder" or "Screenshot placeholder" text in any SVG
- Setlist timing illustration shows two-panel builder with timing bar
- Song library illustration shows searchable song list with titles and durations
- Band collaboration illustration shows stats, actions, and recent setlists
- Illustrations use the app's dark theme palette consistently
- FEAT-02 requirement ("Real app screenshots embedded in feature cards") is satisfied
- User has visually approved the illustrations on the live landing page
</success_criteria>

<output>
After completion, create `.planning/phases/05-landing-page-structure/05-03-SUMMARY.md`
</output>
