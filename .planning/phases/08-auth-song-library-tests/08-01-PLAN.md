---
phase: 08-auth-song-library-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/auth.spec.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Unauthenticated browser visiting /dashboard is redirected to /auth with redirect param preserved"
    - "Authenticated user can load /dashboard without being redirected"
    - "Session persists across page reload (no re-login needed)"
    - "After signing out, user is redirected to /auth and cannot access protected routes"
  artifacts:
    - path: "tests/auth.spec.ts"
      provides: "E2E tests for all auth flows"
      contains: "should redirect"
  key_links:
    - from: "tests/auth.spec.ts"
      to: "tests/fixtures.ts"
      via: "import { test, expect }"
      pattern: "import.*from.*fixtures"
---

<objective>
Write Playwright E2E tests covering all authentication flows: unauthenticated redirect with return URL preservation, authenticated route access, session persistence after reload, and sign-out with post-logout protection.

Purpose: Catch auth regressions -- redirect logic, session handling, and sign-out flow are critical paths that must not break silently.
Output: `tests/auth.spec.ts` with passing tests for AUTH-01, AUTH-02, AUTH-03.
</objective>

<execution_context>
@/Users/pete/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pete/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auth-song-library-tests/08-RESEARCH.md
@tests/fixtures.ts
@tests/smoke.spec.ts
@src/hooks.server.ts
@src/routes/auth/+page.svelte
@src/routes/(app)/settings/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write auth E2E tests</name>
  <files>tests/auth.spec.ts</files>
  <action>
Create `tests/auth.spec.ts` with the following tests, using BDD-style naming ("should [verb] when [condition]"). Import `test` and `expect` from `./fixtures`. Use `test.describe` to group tests by concern.

**Unauthenticated redirect (AUTH-01):**
- `should redirect to login when unauthenticated user visits protected route`: Create a fresh browser context via `browser.newContext({ storageState: undefined })`, navigate to `/dashboard`, assert URL contains `/auth`, extract the `redirect` query parameter and assert it equals `/dashboard`. Close context after.
- `should preserve return URL in redirect`: Same pattern but navigate to `/songs` instead of `/dashboard` to verify the redirect param reflects the actual requested path.

**Authenticated access (AUTH-02):**
- `should access dashboard without redirect when authenticated`: Use default `page` fixture (already authenticated via workerStorageState). Navigate to `/dashboard`, assert URL matches `/dashboard`, assert some page content is visible (e.g. a heading or known element).
- `should maintain session after page reload`: Navigate to `/dashboard`, call `page.reload()`, assert URL still matches `/dashboard` and page content is visible (not redirected to login).

**Sign-out (AUTH-03):**
- `should redirect to auth page after signing out`: Navigate to `/settings`, click the sign-out button (`page.getByRole('button', { name: /sign out/i })`), assert URL contains `/auth`.
- `should block access to protected routes after signing out`: After sign-out (navigate to `/settings`, click sign-out, wait for `/auth` URL), then navigate to `/dashboard` and assert URL contains `/auth` (session is invalidated, auth guard kicks in).

NOTE on sign-out redirect: The actual app redirects to `/auth` after sign-out (not `/` landing page). The code in `settings/+page.svelte` calls `goto('/auth')`. Test the actual behavior.

NOTE on unauthenticated tests: ALWAYS create a fresh context with `storageState: undefined` -- the default `page` fixture has auth injected. Always close the context in a finally block or after assertions.

Each test must be independent -- no shared state. Tests using the default `page` are already authenticated. Tests needing unauthenticated state must create their own context.
  </action>
  <verify>Run `npx playwright test tests/auth.spec.ts` -- all tests pass. If `.env.test` is not populated, verify the file compiles: `npx tsc --noEmit tests/auth.spec.ts` or review the file manually for correctness.</verify>
  <done>auth.spec.ts exists with 6 tests covering redirect (with return URL), authenticated access, session persistence, sign-out redirect, and post-logout route protection. All tests use BDD naming and are fully independent.</done>
</task>

</tasks>

<verification>
- `tests/auth.spec.ts` exists and imports from `./fixtures`
- Tests cover AUTH-01 (unauthenticated redirect + return URL), AUTH-02 (authenticated access + session persistence), AUTH-03 (sign-out + post-logout protection)
- Unauthenticated tests use `browser.newContext({ storageState: undefined })`
- No shared state between tests
- BDD-style test names
</verification>

<success_criteria>
- All auth test scenarios from CONTEXT.md are covered
- Tests are independent and use the fixture system correctly
- Unauthenticated tests properly create fresh browser contexts
- Sign-out test verifies both redirect and session invalidation
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-song-library-tests/08-01-SUMMARY.md`
</output>
