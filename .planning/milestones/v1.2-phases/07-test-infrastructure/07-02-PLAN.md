---
phase: 07-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - tests/helpers/supabase-admin.ts
  - tests/helpers/auth.ts
  - tests/helpers/cleanup.ts
  - tests/fixtures.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "A test user is created via Supabase admin API when a Playwright worker starts"
    - "The test user authenticates through the email/password form on /auth and storageState is saved"
    - "All tests in a worker share the authenticated storageState automatically"
    - "When a worker finishes, the test user and all associated data are deleted"
  artifacts:
    - path: "tests/helpers/supabase-admin.ts"
      provides: "Service role Supabase client singleton"
      contains: "createClient"
    - path: "tests/helpers/auth.ts"
      provides: "Test user creation and deletion helpers"
      contains: "createTestUser"
    - path: "tests/helpers/cleanup.ts"
      provides: "Safe delete helper for per-test cleanup"
      contains: "safeDelete"
    - path: "tests/fixtures.ts"
      provides: "Worker-scoped fixtures with testUser and workerStorageState"
      contains: "test.extend"
  key_links:
    - from: "tests/fixtures.ts"
      to: "tests/helpers/auth.ts"
      via: "import createTestUser, deleteTestUser"
      pattern: "createTestUser"
    - from: "tests/fixtures.ts"
      to: "/auth page"
      via: "UI login with email/password"
      pattern: "signInWithPassword\\|getByLabel.*Email"
    - from: "tests/helpers/auth.ts"
      to: "tests/helpers/supabase-admin.ts"
      via: "import adminClient"
      pattern: "adminClient"
---

<objective>
Create the Supabase admin client, test user lifecycle helpers, worker-scoped Playwright fixtures for authentication, and cleanup utilities.

Purpose: This is the core test infrastructure — every test file imports from fixtures.ts to get an authenticated page and test user. Worker-scoped fixtures create one user per worker, authenticate via the UI, save storageState, and clean up on teardown.
Output: Reusable fixtures and helpers that subsequent test plans import.
</objective>

<execution_context>
@/Users/pete/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pete/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-test-infrastructure/07-RESEARCH.md
@.planning/phases/07-test-infrastructure/07-01-SUMMARY.md
@src/routes/auth/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase admin client and auth helpers</name>
  <files>
    tests/helpers/supabase-admin.ts
    tests/helpers/auth.ts
    tests/helpers/cleanup.ts
  </files>
  <action>
    1. Create `tests/helpers/supabase-admin.ts`:
       - Import `createClient` from `@supabase/supabase-js`
       - Throw if `PUBLIC_SUPABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY` env vars are missing (with clear error message mentioning `.env.test`)
       - Export `adminClient` as a singleton created with the service role key
       - This client bypasses RLS — used by factories and cleanup, never by tests themselves

    2. Create `tests/helpers/auth.ts`:
       - Import `adminClient` from `./supabase-admin`
       - Import `faker` from `@faker-js/faker`

       - Export `createTestUser(workerIndex: number)`:
         - Email format: `test-worker${workerIndex}-${Date.now()}@setlist.test` (per user decision)
         - Password: `faker.internet.password({ length: 20 })`
         - Call `adminClient.auth.admin.createUser({ email, password, email_confirm: true })`
         - Throw on error with descriptive message
         - Return `{ id: data.user.id, email, password }`

       - Export `deleteTestUser(userId: string)`:
         - First delete bands: `adminClient.from('bands').delete().eq('owner_id', userId)` — bands.owner_id has ON DELETE RESTRICT, must delete before user
         - Then delete user: `adminClient.auth.admin.deleteUser(userId)`
         - Both operations warn on error (console.warn) but do NOT throw — cleanup failures should not fail the test run (per user decision)

    3. Create `tests/helpers/cleanup.ts`:
       - Import `adminClient` from `./supabase-admin`

       - Export `safeDelete(table: string, id: string)`:
         - Wraps `adminClient.from(table).delete().eq('id', id)` in try/catch
         - On error: `console.warn(`Cleanup warning [${table}/${id}]:`, error)` — never throws
         - Per user decision: "Cleanup failures warn but don't fail the test run"
  </action>
  <verify>
    - `npx tsc --noEmit tests/helpers/supabase-admin.ts tests/helpers/auth.ts tests/helpers/cleanup.ts` or verify imports resolve
    - Files exist at expected paths with correct exports
  </verify>
  <done>Admin client singleton, test user create/delete helpers, and safe cleanup utility exist and export the documented functions.</done>
</task>

<task type="auto">
  <name>Task 2: Create worker-scoped Playwright fixtures</name>
  <files>
    tests/fixtures.ts
  </files>
  <action>
    Create `tests/fixtures.ts` with worker-scoped fixtures using `test.extend`:

    1. Define types:
       ```typescript
       type TestUser = { id: string; email: string; password: string };
       type WorkerFixtures = {
         testUser: TestUser;
         workerStorageState: string;
       };
       ```

    2. Export `test` extended with worker-scoped fixtures:

       **`testUser` fixture** (scope: 'worker'):
       - Call `createTestUser(workerInfo.workerIndex)` to create user via admin API
       - `await use({ id, email, password })` to provide user to tests
       - Teardown: call `deleteTestUser(id)` to clean up (bands first, then user)

       **`workerStorageState` fixture** (scope: 'worker'):
       - Depends on `browser` and `testUser`
       - Create `fileName` at `path.resolve('tests/.auth', `worker-${workerInfo.workerIndex}.json`)`
       - Ensure `tests/.auth` directory exists (`fs.mkdirSync` with `recursive: true`)
       - Open new page with `storageState: undefined` (no existing auth)
       - Navigate to `/auth`
       - Fill email: `page.getByLabel('Email').fill(testUser.email)`
       - Fill password: `page.getByLabel('Password').fill(testUser.password)`
       - Click submit: `page.getByRole('button', { name: /sign in with email/i }).click()`
       - Wait for redirect: `page.waitForURL('**/dashboard')`
       - Wait for hydration: `expect(page.locator('body')).toBeVisible()` (or a known dashboard element)
       - Save state: `page.context().storageState({ path: fileName })`
       - Close page
       - `await use(fileName)`

       **`storageState` override** (scope: 'test'):
       - Override the built-in `storageState` fixture to use `workerStorageState`
       - `storageState: ({ workerStorageState }, use) => use(workerStorageState)`

    3. Re-export `expect` from `@playwright/test` for convenience:
       ```typescript
       export { expect } from '@playwright/test';
       ```

    All test files will import `{ test, expect }` from `./fixtures` instead of `@playwright/test`.
  </action>
  <verify>
    - `cat tests/fixtures.ts` shows the complete fixture with testUser, workerStorageState, and storageState override
    - TypeScript types are correct (TestUser, WorkerFixtures)
    - Teardown function calls deleteTestUser
    - storageState file path uses workerInfo.workerIndex for isolation
  </verify>
  <done>Worker-scoped fixtures create one test user per worker via admin API, authenticate through the real email/password UI, save storageState for all tests in the worker, and clean up the user on teardown.</done>
</task>

</tasks>

<verification>
1. `tests/helpers/supabase-admin.ts` exports `adminClient` using service role key
2. `tests/helpers/auth.ts` exports `createTestUser` and `deleteTestUser` with correct email format and RESTRICT-aware cleanup ordering
3. `tests/helpers/cleanup.ts` exports `safeDelete` that warns but never throws
4. `tests/fixtures.ts` exports `test` with worker-scoped `testUser` and `workerStorageState`
5. All cleanup failures log warnings but do not throw errors
</verification>

<success_criteria>
- A test importing from `tests/fixtures.ts` gets an authenticated page without any manual auth setup
- Each worker creates exactly one test user with the pattern `test-worker{N}-{timestamp}@setlist.test`
- Worker teardown deletes bands first (RESTRICT), then user (CASCADE handles rest)
- All helpers use the service role client (never anon)
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure/07-02-SUMMARY.md`
</output>
